import { type Config } from 'prettier';

const config: Config = {
    singleQuote: true,
    bracketSpacing: true,
    semi: true,
    arrowParens: 'avoid',
    printWidth: 120,
    bracketSameLine: false,
    trailingComma: 'all',
    tabWidth: 4,

};

export default config;